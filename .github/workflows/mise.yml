name: mise

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  install-tools:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
      - run: prek run --all-files
